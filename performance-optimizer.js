(()=>{const videos=[...document.querySelectorAll('video[data-viewport-video="true"]')];if(!videos.length)return;const reducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)');const play=video=>{if(document.hidden||reducedMotion.matches)return;if(video.preload==='none'){video.preload='metadata';video.load()}const promise=video.play();if(promise&&typeof promise.catch==='function')promise.catch(()=>{})};const pause=video=>{if(!video.paused)video.pause()};if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting)play(entry.target);else pause(entry.target)}},{rootMargin:'700px 0px',threshold:.01});videos.forEach(video=>observer.observe(video))}else{videos.forEach(video=>{video.preload='metadata';play(video)})}document.addEventListener('visibilitychange',()=>{if(document.hidden)videos.forEach(pause);else videos.forEach(video=>{const rect=video.getBoundingClientRect();if(rect.bottom>-700&&rect.top<innerHeight+700)play(video)})})})();